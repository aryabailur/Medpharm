/**
 * @medtrack/ui — shared components across Vayu and Dhanvantari.
 *
 * ARCHITECTURE.md §8.
 *
 * SCAFFOLD: empty. Populate as shared surfaces emerge — the shipment status
 * stepper, temperature chart, risk drilldown panel and evidence panel are all
 * used on both sides.
 *
 * Terminology rule (§1): supplier/manufacturer vs institution. Component names
 * and labels use those words, not "vendor". Fix the words in the UI, not just
 * the pitch.
 */

export {};
